[project]
name = "content-factory"
version = "0.1.0"
description = "Claude Code for Content Creators"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["mcp", "httpx", "uvicorn", "starlette"]
